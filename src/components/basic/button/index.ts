import DashButton from './DashButton.vue';

// // 为组件提供 install 安装方法，供按需引入
// // @ts-ignore
// DashButton.install = function(Vue) {
//   Vue.component(DashButton.name, DashButton);
// };

// 默认导出组件
export default DashButton;